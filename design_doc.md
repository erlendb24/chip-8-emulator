# Chip-8

## How op-codes work

Every op-code starts with a number to identify it, ex. `1nnn`, `1` tells us  
what instruction it is, and `nnn` is the address, the 12 lowest bits of the  
instruction


